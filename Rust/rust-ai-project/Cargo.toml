[package]
name = "rust-ai-project"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ndarray = "0.15"  # For numerical computing
rand = "0.8"      # For random numbers